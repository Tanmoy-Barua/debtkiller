name: App Health

on:
  schedule:
    # Run morning and night health checks in UTC.
    - cron: "0 7,19 * * *"
  workflow_dispatch:

jobs:
  health:
    name: Health check
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run app health check
        run: npm run health
